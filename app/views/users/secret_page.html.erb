<!DOCTYPE html>
<html>
<head>
	<title>Secret page</title>
</head>
<body>
	<header>
		<nav class="nav nav-pills nav-fill">
			<a class="nav-item nav-link active" href="/">Home</a>
			<% if logged_in? %>
				<%= link_to "Log out", "/logout", method: :delete, class: 'nav-link nav-item' %>
			<% else %>
				<a class="nav-item nav-link" href="/new">Registrations</a>
				<%= link_to "Log in", "/login", class: 'nav-link nav-item' %>
			<% end %>
		</nav>
	</header>
	<% if logged_in? %>
	<div class="jumbotron">
		<ul>
			<% @tab_users.each do |user| %>
				<li> <%= user.first_name+ ' ' +user.last_name+ ' = ' +user.email %></li>
			<% end %>
		</ul>
	</div>
	<% else %>
		<h1>You're not a member of our club yet</h1>
		<a href="/login" class="btn btn-lg btn-danger">Log in</a><br/>
		<br/><a href="/new" class="btn btn-lg btn-danger">Register</a>
	<% end %>

</body>
</html>